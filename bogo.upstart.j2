description "bogo flask app"

start on (filesystem)
stop on runlevel [016]

respawn
setuid root
setgid root  
chdir {{repository_path}}

exec gunicorn app:app --bind unix:/tmp/bogo.sock --workers 3 --log-file /var/log/bogo/app.log
